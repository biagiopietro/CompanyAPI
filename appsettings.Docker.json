{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // the server value must match the service of the mysql service inside the dockercompose.yml
    "CompanyDB": "server=mysql-net;port=3306;database=company-dev;user=root;password=password" 
  }
}
